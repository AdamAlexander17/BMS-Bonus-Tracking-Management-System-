# Force LF line endings for shell scripts so they run on Linux even when
# committed from a Windows workstation.
*.sh        text eol=lf
*.bash      text eol=lf

# Keep PowerShell as CRLF (Windows default)
*.ps1       text eol=crlf
*.psm1      text eol=crlf
